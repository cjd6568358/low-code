# Low Code Platform

一个企业级低代码平台，支持多端页面渲染、流程编排、数据管理、运算引擎和细粒度权限控制。SaaS 多租户架构，每租户独立 SQLite 数据库隔离。

---

## ✨ 系统亮点

### 1. 全 Schema 驱动

系统的一切皆 Schema。设计器负责**写** Schema，运行时引擎负责**读** Schema 渲染/执行。设计时与运行时通过 JSON Schema 文件完全解耦，同一份 Schema 可驱动 Web、Mobile、小程序多端渲染。

### 2. 六种资源统一管理

页面、卡片、数据表、流程、自动化、运算 — 六种资源类型全部以 JSON Schema 文件存储在 `tenants/{tenantId}/apps/{appId}/` 下，统一入口、统一格式、统一版本管理。

### 3. 物理级租户隔离

每租户一个独立目录（`tenants/{tenantId}/`），同时包含应用 Schema（JSON 文件）和业务数据（SQLite），完全自包含。不同租户的数据在文件系统层面物理隔离，不存在跨租户数据泄露风险。

### 4. TypeScript 类型即实体

不需要单独的实体定义层。TypeScript 类型系统本身就是数据模型的唯一真相源，`build-tools` 将 TS 类型自动编译为 JSON Schema，供渲染引擎、流程引擎、数据引擎、运算引擎、权限引擎自动映射消费。

### 5. 纯引擎架构

`packages/` 下的引擎层不依赖任何框架（无 Koa、无 React），可独立测试、独立复用。`server/` 负责组装引擎暴露 API，`frontend/` 负责调用 API 渲染 UI。引擎之间通过类型契约协作，不直接耦合。

### 6. 文件即数据源

应用数据不依赖数据库，`tenants/{tenantId}/apps/` 目录就是唯一数据源。API 直接读写 JSON 文件，无数据同步问题。创建应用 = 创建目录，删除应用 = 删除目录，查询应用 = 扫描目录。所有文件可纳入 Git 版本管理、可导入导出、可跨环境迁移。

### 7. UUID 资源标识

所有资源 ID 使用 8 位随机 hex 生成（如 `90ef6d72`），每次创建时校验唯一性。

- **URL/前端**：使用短 ID（无前缀），如 `/app/90ef6d72`
- **目录/数据库记录**：带前缀，如 `tenant_90ef6d72`、`app_90ef6d72`
- **数据库文件**：`tenants/{tenant_xxx}/data/tenant.db`（上级目录已含租户 ID，无需重复）

---

## 📐 系统架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Low Code Platform                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                        多租户层 (Multi-Tenant Layer)                         │
├──────────┬──────────┬──────────┬──────────┬──────────┬──────────────────────┤
│ 渲染引擎  │ 流程引擎  │ 自动化引擎│ 数据引擎  │ 运算引擎  │ 权限引擎             │
├──────────┴──────────┴──────────┴──────────┴──────────┴──────────────────────┤
│                     自动渲染引擎 (Auto-Rendering Engine)                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                       核心服务层 (Core Services)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                   数据存储层 (SQLite per-tenant isolation)                    │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📦 应用管理

用户可创建应用，每个应用包含页面、数据表、流程等资源，支持导出为静态 JSON Schema 文件。

- **应用创建**：支持从空白创建、从已有应用复制、从应用市场导入三种方式
- **应用发布**：草稿应用可发布上线，发布后员工可见可使用
- **页面设计**：可视化拖拽搭建，支持多页面路由配置
- **数据表管理**：可视化创建数据表，配置字段类型、校验规则、索引。数据表默认仅限当前应用使用，需显式开放权限后方可被其他应用引用
- **版本管理**：应用版本快照，支持版本回滚与对比，支持锁定组件库版本确保兼容性
- **配置导出**：导出为 JSON Schema 文件，支持离线部署、版本控制、跨环境迁移
- **数据导出**：支持数据导出（CSV/Excel/SQL）、数据迁移（跨环境/跨库）、数据备份与恢复

> 📄 详见 [应用管理文档](docs/application.md)

---

## 🎨 渲染引擎

涵盖**统一设计器**和**运行时渲染器**。

### 统一设计器

应用资源的统一设计入口，支持页面、卡片、数据表、流程、自动化、运算六种资源类型。路由格式：`/designer/:resourceType/:id`。

- **应用资源概览**（`/designer/app/:id`）：展示应用内所有资源（页面/数据表/流程等），点击进入对应资源编辑器
- **页面设计器**（`/designer/page/:id`）：三栏布局，左侧基础组件面板 / 中间设计区+预览 / 右侧属性配置面板，预览支持 Web / Mobile 切换
- **流程设计器**（`/designer/workflow/:id`）：基于 react-flow-builder 的可视化流程编排，支持 8 种节点类型（开始/结束/审批/条件/并行/延时/通知/自动化），BPMN JSON Schema 导入导出，支持保存/发布
- **其他资源编辑器**（`/designer/card|form|table|automation|computation/:id`）：各资源专属编辑器（待实现）

- **拖拽开发**：基于 DND 实现可视化页面搭建，支持面板→画布添加、画布内拖拽排序，拖拽时显示阴影效果和蓝色放置标准线
- **组件库配置**：默认使用 Ant Design（66 个全量组件），设计器中不可切换。左侧组件面板展示当前组件库的全部可用组件，按 antd 官方分类组织（通用/布局/导航/数据录入/数据展示/反馈）
- **布局容器**：提供行、列、卡片等布局容器组件，支持对基础组件进行灵活布局编排
- **自定义卡片**：由基础组件组合而成的最小化业务组件，支持保存为卡片模板复用；卡片内部可递归嵌套卡片，实现多层业务抽象。对外暴露 Props/Methods/Events/Slots 接口，所有接口均通过设计器 UI 配置（变量选择器、动作链编排、拖拽填充），无需手写代码。详见 [自定义卡片规范](docs/render-engine.md#自定义卡片规范)
- **主题风格配置**：支持全局主题色、圆角、字号、间距等 Token 级配置，实时预览
- **属性自动渲染**：读取组件 TS 类型定义，自动转为 JSON Schema 并渲染属性配置表单，支持按优先级排序、Tab/Group 分组展示
- **条件规则**：支持配置显隐规则控制组件展示，支持条件赋值联动（多条件分支赋值、变量取值、表达式计算）
- **属性定义体系**：`BaseProps` 基础抽象 + TS 继承派生各组件 Props

### 运行时渲染器

- **PageRuntime**：运行时渲染器已实现，路由 `/:tenantId/app/:appId/page/:pageId` 可实际渲染页面（加载 JSON Schema → 递归渲染 antd 组件树）
- **多端支持**：Web / Mobile / 小程序，复用同一套运行时
- **JSON Schema 驱动**：设计器产出的页面描述直接驱动渲染
- **跨端适配层**：适配器模式屏蔽各端差异

### 表单引擎

企业级表单能力，覆盖复杂联动、校验、子表单、特殊控件、数据暂存等场景。

- **联动引擎**：值联动、选项联动、显隐联动、属性联动、跨表单联动，基于 DAG 的循环依赖检测
- **校验引擎**：四层校验（基础/自定义/跨字段/服务端），支持异步校验
- **子表单**：一对多明细表录入，支持增删排序复制，自动汇总
- **特殊控件**：签名、地理位置、扫码、级联选择、关联记录等
- **数据暂存**：自动保存草稿（本地/服务端），防数据丢失

> 📄 详见 [渲染引擎文档](docs/render-engine.md)

---

## 🖼️ 自动渲染引擎

平台基础能力层，提供 **Schema → UI 表单** 的通用渲染能力，供各引擎配置界面复用。**所有基础控件统一使用 Ant Design 组件**，确保样式、交互一致性。

- **Schema 驱动**：任何引擎提供 JSON Schema（含 `x-*` 扩展字段），即可自动生成配置表单，无需手写 UI
- **判别联合**：原生支持 `type` 字段切换不同子配置表单（自动化触发器/动作、流程节点类型等）
- **控件注册表**：基于 antd 的统一控件体系（Input/InputNumber/Select/Switch/DatePicker 等），各引擎可注册自定义控件（条件构建器、表达式编辑器、权限矩阵等）
- **变量绑定集成**：字段旁自动注入变量绑定按钮，统一变量选择器支持静态值/变量引用/表达式三种模式
- **字典集成**：通过 `x-dictionary` 引用系统字典，枚举值自动填充，无需 Schema 硬编码
- **布局模式**：支持折叠分组、标签页、分步表单、平铺区块四种布局，通过 `x-layout-mode` 切换

> 📄 详见 [自动渲染引擎文档](docs/auto-rendering-engine.md) | [表单引擎文档](docs/form-engine.md) | [表单运行时架构](docs/form-runtime-architecture.md)

---

## ⚙️ 流程引擎

支持业务流程的可视化编排与自动化执行。

- **触发方式**：表单按钮（主要） / Webhook / 定时任务
- **流程能力**：条件分支、并行分支、排他网关、人工审批、数据操作、API 调用
- **节点级快照**：每个节点流出时自动捕获数据快照，形成完整变更链，支持任意节点溯源与对比
- **节点表单**：每个审批节点可独立配置表单视图，控制字段的只读/可编辑/隐藏权限
- **流程版本化**：流程定义支持版本管理，运行中实例绑定创建时版本，支持版本迁移

> 📄 详见 [流程引擎文档](docs/workflow-engine.md)

---

## 🤖 自动化引擎

基于 ECA（事件-条件-动作）模型的自动化规则引擎，支持配置条件规则，在满足条件时自动触发流程引擎、发送通知或执行数据操作。

- **触发器**：数据变更 / 定时任务 / 表单事件 / 审批事件 / 自定义事件
- **条件配置**：多条件 AND/OR 组合、嵌套条件组、运算引擎集成
- **动作类型**：触发流程、发送通知（多渠道）、数据操作、API 调用、Webhook
- **执行控制**：触发冷却、每日上限、生效时段、失败重试
- **执行日志**：完整记录触发事件、条件求值结果、动作执行状态与耗时

> 📄 详见 [自动化引擎文档](docs/automation-engine.md)

---

## 📊 数据引擎

基于 SQLite 字段类型封装为面向业务的格式化字段。

- **格式化字段**：邮箱、手机号、地址、日期区间、身份证、金额等
- **数据流转**：流程引擎驱动 / 自动化引擎驱动 / 运算引擎驱动 / 实时同步

> 📄 详见 [数据引擎文档](docs/data-engine.md)

---

## 🧮 运算引擎

融合数据库操作与 JavaScript 原生能力的声明式数据运算。

- **数据库运算**：聚合、条件查询、关联查询、聚合管道
- **JS 数据方法**：字符串、数组、数学、日期等原生方法支持
- **运算场景**：字段联动、数据统计、流程条件、自定义公式

> 📄 详见 [运算引擎文档](docs/computation-engine.md)

---

## 🏢 多租户架构

SaaS 多租户模式，每个租户独立 SQLite 数据库文件，互不干扰。

- **租户隔离**：每租户独立 `.db` 文件，数据物理隔离
- **配额管理**：按套餐控制用户数、应用数、存储空间
- **计量计费**：API 调用量、存储使用量统计
- **租户级配置**：字典、角色、权限、组织架构独立管理

---

## 🖥️ 租户管理后台

SaaS 模式下的租户级管理控制台，包含**工作台**、**应用中心**、**流程中心**、**配置中心**三大模块。

### 应用中心

- **应用列表**：管理租户内所有应用，支持创建/发布/归档/启用/停用
- **应用资源**：查看和管理应用内的页面、实体、数据表、流程、自动化规则
- **应用设置**：组件库锁定、访问控制、数据保留策略、API 开放配置
- **应用管理员**：为应用分配一个管理员，负责该应用的页面/流程/运算等资源的配置

### 流程中心

- **工作流配置**：审批流程模板、审批人规则（指定人员/岗位/部门主管/发起人自选）、超时策略、回退策略
- **协作任务**：任务分配、状态跟踪、关联应用资源
- **通知配置**：多渠道通知模板、通知规则、免打扰时段

### 配置中心

- **组织架构**：部门管理（架）、岗位管理（岗）、人员管理（人），支持平台原生创建 + 第三方同步
- **租户级字典**：租户自定义字典，供租户内所有应用引用
- **OpenKey**：API 访问密钥管理，控制外部系统的数据访问范围和限流策略
- **应用管理员**：配置应用级管理员，控制其对应用页面/流程/运算的管理权限

> 📄 详见 [租户管理后台文档](docs/tenant-admin.md)

---

## 🔐 权限引擎

RBAC 模型实现细粒度数据鉴权。

- **权限维度**：菜单权限、按钮权限、数据权限、字段权限
- **鉴权流程**：身份认证 → 角色解析 → 权限匹配 → 数据过滤

> 📄 详见 [权限引擎文档](docs/permission-engine.md)

---

## 📋 安全审计日志

记录平台关键操作，满足企业安全合规要求，支持平台/应用管理员分级查看。

- **分级审计**：平台级操作全量记录（不可关闭），应用级操作可配置记录级别（关闭/仅关键/全量）
- **审计事件**：认证、授权、数据操作、配置变更、流程审批等全覆盖
- **管理能力**：多维度筛选查询、日志详情查看、导出能力
- **存储策略**：按月分区、可配置保留周期、冷热分离

> 📄 详见 [安全审计日志文档](docs/audit-log.md)

---

## 🔗 第三方组织架构集成

对接企业微信、钉钉、飞书、HR 系统、LDAP/AD，自动同步组织架构、岗位、员工信息。

- **通用适配器**：统一 `OrgAdapter` 接口，内置企业微信/钉钉/飞书/HR API/LDAP 实现，支持自定义扩展
- **同步策略**：定时拉取 + Webhook 实时推送双模式，增量同步 + 冲突处理
- **数据映射**：可配置字段映射，支持枚举值转换
- **管理能力**：连接配置、同步日志、错误告警、手动触发

> 📄 详见 [组织架构集成文档](docs/org-integration.md)

---

## 📬 消息中心

平台统一消息服务，支持多渠道消息触达与消息生命周期管理。

- **多渠道**：站内消息、邮件、短信、企业微信/钉钉/飞书推送
- **消息模板**：可视化模板编辑，支持变量插值，多渠道内容适配
- **订阅偏好**：用户自定义接收渠道，支持免打扰时段，紧急事件强制推送
- **状态管理**：已读/未读状态、未读计数、系统公告、定时发布

> 📄 详见 [消息中心文档](docs/message-center.md)

---

## 🏪 应用市场

应用模板的发布、审核、搜索、评分、版本管理，促进低代码应用的复用与共享。

- **模板管理**：发布/审核/上架/下架全生命周期，自动检测 + 人工审核
- **发现与推荐**：分类筛选、关键词搜索、热门/精选/个性化推荐
- **评分评论**：多维评分（功能/易用/设计/文档），用户评论与发布者回复
- **版本管理**：语义化版本号，升级兼容性检测，数据迁移

> 📄 详见 [应用市场文档](docs/app-marketplace.md)

---

## 🔌 开发者扩展与集成

插件系统、事件总线、Webhook、SDK 和 API 文档，支持二次开发与外部系统集成。

- **插件系统**：组件/流程节点/数据源/函数/主题五类插件，安全沙箱隔离
- **事件总线**：平台内部事件解耦通信，支持插件订阅
- **Webhook**：出站事件推送，签名验证，重试策略
- **REST API**：OpenAPI 3.0 规范，统一响应格式和错误码
- **SDK**：JavaScript/TypeScript SDK，封装常用 API 调用

> 📄 详见 [开发者扩展文档](docs/developer-extension.md)

---

## 🛠️ 技术栈

| 层级 | 技术选型 |
|------|---------|
| 前端框架 | React 18 / TypeScript 5 |
| 构建工具 | Vite 5 |
| 组件库 | Ant Design 5 |
| 路由 | React Router 6 |
| 后端框架 | Koa 2 |
| 数据库 | SQLite（koffi FFI 直调 sqlite3.dll，无需原生编译） |
| 多租户 | 租户级 SQLite 文件隔离（每租户一个 .db） |
| 流程引擎 | react-flow-builder + 自研 BPMN 2.0 引擎 |
| 包管理 | Yarn 1.22 Workspaces |
| 测试 | Vitest 2 |
| 认证集成 | LDAP / AD / OAuth2 / SAML 2.0 |

---

## 📁 项目结构

```
low-code/
├── frontend/                          # 前端门户（Vite + React，端口 5173）
│   └── src/
│       ├── auth/                      # 认证（AuthContext、ProtectedRoute、mockAuth）
│       ├── components/                # 通用组件（PermissionGuard、PageRuntime）
│       ├── designers/                 # 资源设计器（PageDesign、TableDesign、WorkflowDesign、AutomationDesign）
│       ├── layouts/                   # 布局（MainLayout 侧边栏+顶栏）
│       ├── pages/                     # 页面（LoginPage、WorkspacePage、AppCenterPage、WorkflowCenterPage、ConfigCenterPage、AppDesignPage）
│       └── styles/                    # 全局样式
│
├── server/                            # 后端 API（Koa，端口 3001）
│   └── src/
│       ├── config/                    # 配置（端口、数据库路径、DB 单例）
│       ├── middlewares/               # 中间件（错误处理、CORS、日志）
│       ├── routes/                    # 路由（auth、health、workflows、workflow-instances、workflow-tasks）
│       └── services/                  # 业务服务层（FileDatabaseAdapter、FileSnapshotService、WorkflowService）
│
├── packages/                          # 引擎层（纯逻辑，无框架依赖）
│   ├── shared/                        # 公共类型、权限引擎
│   ├── computation/                   # 运算引擎
│   ├── renderer/                      # 渲染引擎 + 设计器 + 流程 UI
│   ├── auto-rendering/                # 自动渲染引擎
│   ├── data/                          # 数据引擎（SQLite koffi FFI）
│   ├── build-tools/                   # 构建工具（TS → JSON Schema 编译器）
│   ├── workflow-bpmn/                 # BPMN 2.0 类型定义、校验器、序列化器
│   └── workflow/                      # 流程引擎核心（WorkflowEngine、StateMachine、SnapshotEngine）
│
├── tenants/                           # 租户数据（每租户独立目录）
│   └── tenant_{uuid}/                 # 目录名带前缀，uuid 为 8 位 hex
│       ├── tenant.json                # 租户元数据（含 uuid 字段）
│       ├── apps/                      # 应用 Schema
│       │   └── app_{uuid}/            # 目录名带前缀
│       │       ├── app.json           # 应用元信息（含 schemaVersion、version、expose）
│       │       ├── pages/             # 页面 Schema
│       │       ├── cards/             # 卡片 Schema
│       │       ├── tables/            # 数据表 Schema
│       │       ├── workflows/         # 流程 Schema
│       │       ├── automations/       # 自动化 Schema
│       │       ├── computations/      # 运算 Schema
│       │       └── dist/              # 发布产物
│       │           └── app.bundle.json
│       ├── uploads/                   # 上传文件（图片、文档等，跨应用共享）
│       └── data/                      # 租户 SQLite 数据库
│           └── tenant.db
│
├── data/                              # 系统级数据
│   ├── _system.db                     # 平台管理员、套餐配置
│   └── dictionaries/                  # 全局字典（JSON 格式）
│
├── docs/                              # 设计文档（20+）
├── scripts/                           # 构建/生成脚本
│   └── generate-antd-schemas.ts       # antd TS → JSON Schema 自动生成
├── TODO.md                            # 技术难点与工作计划
└── memory/                            # 会话记忆
```

### 架构分层

```
frontend/        ← 应用层（React UI，调用 API）
server/          ← 核心服务层（Koa，组装引擎，暴露 API）
packages/*       ← 引擎层（纯逻辑，可独立测试）
tenants/         ← 租户数据（Schema + SQLite，文件系统即数据源）
data/            ← 系统级数据（平台管理员、套餐配置）
```

---

## 🚀 快速开始

```bash
# 安装依赖
yarn install

# 生成演示数据（山水集团）
npx tsx packages/data/scripts/seed-shansui.ts

# 启动前端 + 后端（同时启动）
yarn start:all

# 或分别启动
yarn server     # 后端 API（:3001）
yarn frontend   # 前端门户（:5173）

# 构建所有包
yarn build

# 运行测试
yarn test
```

### 演示账号

| 角色 | 邮箱 | 密码 |
|------|------|------|
| 租户管理员 | admin@shansui.com | shansui123 |
| 员工 | zhangsan@shansui.com | shansui123 |

### 登录页

- 平台管理员：`http://localhost:5173/login`
- 租户登录：`http://localhost:5173/:tenantId/login`（如 `/tenant_90ef6d72/login`，展示租户名称和图标）

### 路由规范

所有租户页面路由必须带 `/:tenantId` 前缀：`/:tenantId/workspace`、`/:tenantId/apps`、`/:tenantId/workflows`、`/:tenantId/config`、`/:tenantId/designer/:resourceType/:id`

---

## 📄 License

MIT
sk-c16jjwa3jyk2y5kj1srt01hh62xhamqgyxolf2oij3ktmukx

sk-cgrh1lxmczoqqa0j9yaei6rf58ofc7pqoahw5hbyerj8r4s5