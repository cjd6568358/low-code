/**
 * 组件库定义
 *
 * antd 为默认组件库，提供组件实现、分类映射、JSON Schema。
 * 新增组件库只需实现同结构的文件并在 Designer 中传入 library 名即可。
 */

export {
  antdComponentImpls,
  antdCategoryMap,
  antdContainerTypes,
  antdSchemas,
} from './antd';
