{
  "name": "@low-code/automation",
  "version": "1.0.0",
  "description": "自动化引擎 — 基于 ECA 模型的规则引擎",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@low-code/computation": "workspace:*",
    "@low-code/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "typescript": "^5.7.3"
  }
}
